# The active flavor is per-machine state switched by `dotfiles theme <name>`
# (the repo stays normalized to ayu-dark; backup resets this line). Flavors
# live in yazi/flavors/, generated from themes/*/palette.toml — regenerate
# with `dotfiles theme render`, never edit them by hand.

[flavor]
dark = "ayu-dark"
