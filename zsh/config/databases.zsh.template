# TEMPLATE generated by `dotfiles backup` — values were removed.
# Fill in and save as the real file (chmod 600). Lines marked `# public` keep their value.
# Database connection strings.
#
# Any NVIM_DB_<NAME> variable is picked up automatically by Neovim
# (lua/config/options.lua) and exposed as a connection named <name> in
# vim-dadbod-ui and lazysql. Adding a database is one export
# here -- no change needed on the Neovim side.
#
# Uncomment and fill in. Names are lowercased, so NVIM_DB_DEV -> "dev".

export NVIM_DB_GRIDSCAN_LOCAL=
export NVIM_DB_GRIDSCAN_DEV=
